name: mokimo-website

services:
  mokimo-website:
    image: nginx:alpine
    container_name: mokimo-website
    restart: unless-stopped
    volumes:
      - .:/usr/share/nginx/html:ro
    networks:
      - business

networks:
  business:
    external: true
    name: business
